{
  "name": "dynamodb-local-tester",
  "version": "1.0.0",
  "description": "docker run -p 8000:8000 --name dynamodb amazon/dynamodb-local",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1197.0"
  }
}
